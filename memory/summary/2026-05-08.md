# Summary - 2026-05-08

- source: /home/cheche/.openclaw/workspace-chenlu/memory/2026-05-08.md
- generated_at: 2026-05-09 23:35:13 CST
## Key Lines (max 30)
- 现象：公网访问 47.107.183.171:80 返回 502 Bad Gateway
- 根因：nohup 手动进程 和 systemd 服务同时抢占 8080 端口 → 互相挤崩溃
- 修复：干掉所有旧进程，仅保留 systemd 单一管理，`Restart=always` + `RestartSec=2`
- 公网链路：你 → 47.107.183.171:80 → nginx → localhost:8080 → Python
- `dashboard-healthcheck.sh`：改为 `systemctl --user restart` 启动，避免 nohup 冲突
- `evening-check.sh`：加上 gateway 自动修复（检测到挂了自动 `systemctl restart`）
- 问题：`output_dir` 映射到 `/home/cheche/晨露产出物/`（通用目录，文件旧且少）
- 修复：改回 `/home/cheche/.openclaw/workspace-chenlu/晨露产出物/`
- 结果：Deliverables 从 2 个变成 14 个真实交付物
- 问题：6 个 Bot 卡片背面的产出物列表是 HTML 模板写死的，不会随真实产出更新
- 修复：HTML 添加占位符 `{{CHARLES_FILES}}` 等，Python 脚本动态替换为真实文件列表
- `nohup` 和 `systemd` 不能混用管理同一个进程，会抢端口
- Dashboard 类单页应用密码验证必须纯前端（localStorage），后端拦截会导致返回 JSON 而不是 HTML
- 产出物目录映射必须用工作区路径（`workspace-chenlu/`），不能用通用路径
- systemd: `active (running)` PID 196563
- 内存: 10.8MB（稳定）
- nginx: 正常转发
