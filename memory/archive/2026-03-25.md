# 2026-03-25 日志

## 系统排查与配置整理
- **模型报错排查**: 协助公主排查了因为 `fox-gemini` 服务端异常导致的大模型 500 报错。
- **实例配置梳理**: 梳理了当前系统中所有的活动实例（`main`, `bot2`, `sunny`, `mumu`）的模型配置文件，发现 `mumu` 缺失配置，并且其他实例之间存在 API Keys、Base URL 和 Provider 配置不统一且混乱的情况。
- **权限安全排查**: 协助排查了因为陌生人调戏导致 `bot2` 产生 `Pairing` 请求报错的问题，解答了安全拦截机制。

## 早报脚本重写与迭代
- **背景**: 公主反馈早报脚本之前的内容每天都一样（硬编码数据）。
- **第一版**: 重写了 `morning_report.py`，接入 `Tavily API Key` 动态抓取内容。
- **第二版 (中文蓝调版)**: 公主要求拒绝英文、邮件排版使用蓝色。加入了 `gemini-3.1-pro` 作为内容翻译引擎，并将样式改为晨露专属蓝。
- **第三版 (高级设计师版)**: 公主提供了一套 `顶级 Newsletter 视觉设计师` 提示词。将其封装为了 `skills/newsletter-designer` 技能，严格锁定 700px 定宽、无缝图片和 [LUR] 晨露蓝风，全面重构了排版代码。
- **第四版 (纯正抗英版)**: 针对 GitHub 模块依旧有英文漏网之鱼的问题，修改了 Tavily 搜索前缀（强制带 `中文盘点` 等词），并将大模型翻译的 `temperature` 调至 0.3 并换用了更严厉的 Prompt 彻底消灭英文。

## 技能与配置更新
- **USER.md**: 记录了公主“排版必用晨露蓝”、“绝对不能出现英文”的两条硬性偏好。
- **Skills**: 新增了 `newsletter-designer` 技能。