# 2026-03-24 日志

- **Rainbow 模型切换故障排查**：公主想要将 Rainbow 的默认模型切换为 `fox-gemini/gemini-3.1-pro`。经过排查发现 `models.json` 的配置应该在全局 `openclaw.json` 中配置，但配置并重启 Gateway 后该 provider 仍未生效，怀疑是不支持该 API 类型或是缓存问题，留待明日继续排查。
- **Workspace GitHub 备份**：对 chenlu 的 workspace 进行了本地 Git 备份（排除了敏感文件），但需要公主提供 GitHub token 才能推送到远程仓库。
- **飞书插件冲突**：尝试通过 `feishu_create_doc` 发送日报时，遇到新老插件冲突（`openclaw-lark` 未被完全禁用），无法成功创建飞书文档，留待明日修复插件环境。
- **日报自动发送系统**：发现之前其实没有真正自动撰写和发送日报的脚本（只有个读新闻的），于是先配了个 22:05 的 cron 提醒脚本。
- **Self-improving 技能落地**：通过在 `HEARTBEAT.md` 中增加触发规则，并建立 `/self-improving/` 下的目录和文件，让自我进化技能得以实际生效。记录了一条反思：必须开发全自动读取记忆、生成日报并推送的 Python 脚本。
